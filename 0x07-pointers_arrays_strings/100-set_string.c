#include "main.h"

/**
 * set_string - funciton to return the address of an array
 *
 * @**s: the first one
 * @*to: the second one
 *
 * Return: pointer to an array or 0
 */

void set_string(char **s, char *to)
{
		*s = to;
}
