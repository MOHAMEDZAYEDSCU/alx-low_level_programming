#include "main.h"

/**
 * print_numbers - to print number from 0 to 9
 * Description: no thing elseto write
 * Return: 0 Always
 */

void print_numbers(void)
{
	int i;

	for (i = 0 ; i <= 9 ; i++)
		putchar(i);
	putchar('\n');

	return (0);
}
