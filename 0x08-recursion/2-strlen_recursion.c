#include "main.h"

/**
 * _strlen_recursion - funciton to print strings
 *
 * Description: no more words to say
 * @s: pointer to an arary
 *
 * Return: no return for void function
 */

int _strlen_recursion(char *s)
{
	int x = strlen(s);

	return (x);
}
